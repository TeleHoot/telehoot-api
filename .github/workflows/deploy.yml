name: Dev Deploy
on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  quality:
    uses: TeleHoot/telehoot-api/.github/workflows/code-quality.yml@dev
  tests:
    uses: TeleHoot/telehoot-api/.github/workflows/tests.yml@dev
  deploy:
    runs-on: ubuntu-latest
    needs: [quality, tests]
    steps:
      - uses: actions/checkout@v4
      - uses: StalinidzeCorp/ycr-push@main
        env:
          KEY_JSON: ${{secrets.KEY_JSON}}
          REGISTRY_ID: ${{secrets.REGISTRY_ID}}
          DOCKERFILE_PATH: ./docker/Dockerfile.prod
          IMAGE_NAME: telehoot-api
          IMAGE_TAG: latest
